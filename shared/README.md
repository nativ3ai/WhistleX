# Shared Types

Lightweight TypeScript package that centralizes domain interfaces shared across the monorepo.

## Usage

```ts
import { IntelPoolSummary } from "@intel-marketplace/shared";
```

Use `npm install` at repo root to build the type declarations.
