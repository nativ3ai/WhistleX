# Scripts

Placeholder for operational scripts such as contract deployments, Supabase migrations, and data backfills.

Suggested entries:

- `deploy-factory.ts` – deploy `IntelPoolFactory` via Hardhat.
- `sync-schema.ts` – apply Supabase SQL files to local or remote instances.
- `seed-sample-data.ts` – insert example pools for demos.
